import { FilterByTabPipe } from './filter-by-tab.pipe';

describe('FilterByTabPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTabPipe();
    expect(pipe).toBeTruthy();
  });
});
